[project]
name = "voice-clone-studio"
version = "0.1.0"
description = "Local Russian voice design and voice cloning with Qwen3-TTS on Apple Silicon"
requires-python = ">=3.11,<3.12"
dependencies = [
  "gradio>=5.0,<7",
  "mlx-audio>=0.5.3,<0.6",
  "soundfile>=0.13,<1",
  "websockets>=15,<16",
]

[tool.uv]
package = false
